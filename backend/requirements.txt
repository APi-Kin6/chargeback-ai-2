# backend/requirements.txt
# ── CORE BACKEND FRAMEWORK ──
fastapi==0.110.0
# The pinned version that resolves the build error on Render
pydantic==2.6.3
uvicorn==0.27.1

# ── DATABASE & ORM ──
sqlalchemy==2.0.27
# Python driver for PostgreSQL (Render uses this)
psycopg2-binary==2.9.9

# ── CONFIG & SECRETS ──
pydantic-settings==2.2.1
python-dotenv==1.0.1
# Required for JWT (authentication)
python-multipart==0.0.9

# ── AI/LLM SERVICE ──
anthropic==0.21.3

# ── UTILITIES ──
# Required by FastAPI for some HTTP exception handling
starlette==0.36.3