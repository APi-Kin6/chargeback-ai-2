# ── 2. FRONTEND (Vite/React — Static Site) ───────────────────────────────
  - type: web
    name: chargeback-frontend
    runtime: static
    # region: oregon      <-- Already removed (good)
    # plan: free          <-- REMOVE THIS LINE ENTIRELY
    buildCommand: |
      cd frontend
      npm install
      npm run build
    staticPublishPath: ./frontend/dist
    autoDeploy: true